{
  "name": "promptagent",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "bun run --bun tsc --noEmit",
    "optimize": "bun run src/cli/optimize.ts",
    "generate": "bun run src/cli/generate.ts",
    "eval:dist": "bun run src/cli/eval-dist.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "p-limit": "^7.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.2",
    "@mastra/core": "^0.24.9",
    "@mastra/evals": "^0.14.4",
    "zod": "^4.3.5"
  }
}
